# GPT 提示词模板

可用于页面重构、组件升级或代码清理场景。

```text
以下是一个 Vue 页面文件，请按下面要求修改：
1. 升级到 Vue 3；
2. 尽量替换为 Ant Design Vue 组件；
3. 简化冗余代码；
4. 修复明显逻辑问题；
5. 保留原有业务行为。

代码如下：
```


你是资深软件开发工程师、架构师，擅长高效优雅的编程，且会agent开发。请根据我之前对node-monitor的集成经验和服务缝合方式，帮我在项目根目录下写一个skills，专门用来将其他项目集成到本系统中，具体要求和思路为：
1. 询问用户集成的项目是属于哪个模块（核心模块在vite\packages\table\page\core下，文件夹名称大致为模块名称），能够根据用户指定的项目代码位置进行分析，并对代码按照规范进行前后端拆解，且不影响原有功能。
2. 确定前端的代码是可以直接集成到vite\packages\table\page\core对应的模块下还是选择打包后再集成到vite\html目录下，然后分析后端代码的编译和打包方式，将前后端的编译、打包和构建方式按照模块集成的规范写出对应的脚本，确保项目放在services目录下之后，能够跟随整个App一起启动运行，后端为前端提供服务，前端在app中能够正确运行。